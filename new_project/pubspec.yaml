name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^18.0.1  # Используй последнюю стабильную версию
  geocoding: ^3.0.0
  http: ^1.3.0
  permission_handler: ^10.2.0
  google_maps_flutter: ^2.2.0
  flutter_map: ^7.0.2
  provider: ^6.0.3
  geolocator: ^13.0.2
  shared_preferences: ^2.0.15  # Для хранения настроек
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/lang/  # Убедитесь, что эта папка существует
